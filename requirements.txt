# ============================================================================
# ApSense / ra_studies 服务器训练环境
# ============================================================================
# 推荐 Python 3.10+，CUDA 11.8 / 12.1 兼容
# 安装命令：pip install -r requirements.txt
# 如果服务器有 conda，建议先建一个干净的 env：
#     conda create -n apsense python=3.10 -y && conda activate apsense
# GPU 版 PyTorch 建议按官网命令单独装（带 cu 后缀），不要从这个 requirements 装。
# ============================================================================

# ── 训练核心（model_v2 必需）─────────────────────────────────────────────────
torch>=2.0          # 单独装 GPU 版：pip install torch --index-url https://download.pytorch.org/whl/cu121
numpy>=1.23
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65
hydra-core>=1.3
mlflow>=2.8
shap>=0.43
imbalanced-learn>=0.11

# ── 预处理补跑用（仅当服务器要重新跑 preprocess_modular.py 时需要）───────────
# 如不打算在服务器补跑预处理，可以注释掉以下两行
neurokit2>=0.2
pyedflib>=0.1.36
